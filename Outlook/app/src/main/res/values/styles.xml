<resources>
    <!-- Base application theme -->
    <style name="Theme.outlook" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="toolbarStyle">@style/CustomToolbarStyle</item>
    </style>

    <!-- Customize Toolbar appearance -->
    <style name="CustomToolbarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleTextAppearance">@style/CustomToolbarTitleText</item>
    </style>

    <!-- Customize Toolbar Title text appearance -->
    <style name="CustomToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!-- Change text size, color, and font here -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <!-- <item name="android:fontFamily">@font/your_custom_font</item>  Use a custom font -->
        <item name="android:textSize">20sp</item>
    </style>
</resources>
